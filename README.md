## Cfhighlander component for single ec2 instance


**TL;DR**

```shell
gem install cfhighlander && \
  cfpublish github:toshke/cfhl-component-simple-instance
```

Or use the `template.yaml` file already provided
within the repository root. 
